import { Config } from './core/interfaces/common.interfaces';

export const config: Config = {
  version: '0.1',
  projectName: 'BibleExplorer',
  defaultVersionId: 'kjv',
  defaultLanguage: 'en',
};
